events{}

http{
    include /etc/nginx/mime.types;

    upstream servers{
        server backend1:8080;
        server backend2:8080;
    }

    server{
        listen 80;

        location / {
            proxy_pass http://servers;
        }
    }
}